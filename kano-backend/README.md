# kano-backend

Flask + SQLAlchemy API service for the Kano study tool.

See the monorepo root `README.md` for the quickstart. This package uses Poetry
with a src-layout; the real Flask application factory lands in Story 1.3.
