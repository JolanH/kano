# Base URL of the Kano API, including the /api/v1 prefix.
# When the backend is started with `docker compose up` from the repo root,
# Flask listens on port 5000 inside the container; the dev/manual path also uses :5000.
KANO_API_BASE=http://localhost:5000/api/v1
